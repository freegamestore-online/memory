{
  "name": "memory",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @memory/web dev",
    "build": "pnpm --filter @memory/web build",
    "preview": "pnpm --filter @memory/web preview",
    "typecheck": "pnpm --filter @memory/web exec tsc -b"
  }
}
